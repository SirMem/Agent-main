# API base URL used by frontend requests.
# Docker/Nginx deployment (api.js already includes /api): leave empty
# Local direct backend: http://localhost:8066
VITE_API_BASE=

# Vite dev-server proxy target (used by `npm run dev`).
VITE_API_PROXY_TARGET=http://localhost:8066
